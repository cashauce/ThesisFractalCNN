# Function to compress and evaluate images in a folder using enhanced compression
def run_enhanced_compression(original_path, output_path, limit, block_size=8):

    print(f"***Finished compressing {limit} image/s***")
